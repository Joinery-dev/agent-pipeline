{
  "name": "claude-agent-pipeline",
  "version": "0.1.0",
  "description": "Multi-agent build pipeline for Claude Code — PM, Builder, QA, and Resolver agents with schema-enforced state tracking",
  "type": "module",
  "bin": {
    "agent-pipeline": "bin/init.js"
  },
  "scripts": {
    "postinstall": "playwright install chromium"
  },
  "keywords": [
    "claude-code",
    "agents",
    "pipeline",
    "project-management",
    "qa",
    "build-system"
  ],
  "license": "MIT",
  "dependencies": {
    "@playwright/test": "^1.50.0"
  },
  "files": [
    "bin/",
    "lib/",
    "template/",
    "README.md"
  ]
}
